
'use client';
import React from 'react';

import { Chip } from '@atlrdsgn/kit';

export const DemoChip = () => {
  return (
    <Chip
      size={'small'}
      variant={'hyper'}
      shape={'pill'}>
      Small Chip
    </Chip>
  );
};