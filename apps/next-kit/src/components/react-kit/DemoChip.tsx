
'use client';
import React from 'react';

import { Chip } from '@atlrdsgn/kit';

export const DemoChip = () => {
  return (
    <Chip
      size={'medium'}
      variant={'slate'}
      shape={'pill'}>
      Small Chip
    </Chip>
  );
};